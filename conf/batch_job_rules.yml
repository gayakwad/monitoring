groups:
- name: batch_job_rules.yml
  rules:
  - alert: MyBatchJobNotCompleted
    expr: my_batch_job_duration_seconds > 2
    for: 1m
    labels:
      severity: page
    annotations:
      description: ""
      summary: MyBatchJob has not completed successfully in over an hour
